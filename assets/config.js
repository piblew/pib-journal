// Adjust this constant before deploying your frontend.
// When you deploy backend to Render it will have a URL like https://your-backend.onrender.com
// Set API_BASE_URL to that URL (no trailing slash).
// For local testing use e.g. "http://localhost:3000"
const API_BASE_URL = 'https://REPLACE_WITH_YOUR_RENDER_URL';